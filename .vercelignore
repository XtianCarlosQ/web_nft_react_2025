# Ignore backup and disabled API files so Vercel doesn't count them as functions
api/backups/
disabled_api/

# Defensive: ignore any files named with 'copy' (created during development)
api/*copy*
api/*copy*.*

# Ignore old auth folder to avoid duplicate functions; keep top-level api/auth.js
api/auth/
!api/auth.js

# Node modules and build artifacts
node_modules/
dist/
build/
